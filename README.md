# Jev Research

The full MVP was developed against write access on `vlasvar/greek-scrabble`.

**Please rename** `vlasvar/greek-scrabble` → `jev-research` in GitHub settings (Settings → General → Repository name), then delete this empty placeholder if it conflicts.

Branch with the MVP: `cursor/jev-research-mvp-fa8c` on greek-scrabble until rename completes.
