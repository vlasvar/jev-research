# Required for Jev triage (server-side only — never expose to the browser)
TYPESAFE_API_KEY=

# Optional overrides
# TYPESAFE_MODEL=jev-latest
# TYPESAFE_BASE_URL=https://api.typesafe.ai
# PORT=8787
# HOST=127.0.0.1
# DATA_DIR=./data
# CODEX_BIN=codex
# SEARCH_PROVIDER=duckduckgo

# Do NOT set OPENAI_API_KEY or CODEX_API_KEY for the normal MVP path.
# Jev Research requires ChatGPT-authenticated Codex (subscription entitlement).
